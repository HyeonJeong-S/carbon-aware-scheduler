# -*- coding: utf-8 -*-
"""그림 전체가 공유하는 회색 팔레트 (2026-09-24, b6 배분 — 열 장 통합 점검에서 발견).

문제: 그림마다 회색이 제각각 헥스값이었다 — #999999(그림5 홈 유지분) · #8c8c8c
(그림7 상한 도달) · #595959(그림7 마감 강제) · #bfbfbf(그림8 실현 불가 반사실) ·
#f0f0f0(그림10 위반 허용 구간 배경). 뜻이 겹치는 자리는 없어 충돌은 아니었지만
"한 사람이 그린 것"처럼 보이지 않았다.

해법: 뜻에 따라 넉 단계로만 정하고, 그림들이 이 파일의 상수를 가져다 쓴다(파일마다
헥스를 새로 박지 않는다). 흑백 인쇄에서 다섯 단계 이상은 구별이 안 되므로 넷을
넘기지 않는다 — b6 지시.

  BG_WASH       배경 음영(그림 10 "위반 300건 이하" 띠) — 가장 옅다. 그 위에 검정
                표식·글자가 얹히므로 대비를 크게 남겨야 한다.
  AUX_GRAY      보조·부수(그림 5 홈 유지분 세로선) — 주 계열(검정)을 가리지 않는
                곁가지 정보.
  EXCEPT_GRAY   정상 대비 예외(그림 7 상한 도달·배치 불가, 그림 8 실현 불가 반사실)
                — "이 값은 보통 경로가 아니다"를 뜻한다.
  CRITICAL_GRAY 가장 예외적인 것(그림 7 마감 강제 편입) — EXCEPT_GRAY보다 한 단계
                더 예외적인 사건. EXCEPT_GRAY와 나란히 놓이는 유일한 자리(그림 7)라
                인쇄 크기 그대로 렌더해 구별을 직접 확인했다(2026-09-24).

값은 240·191·140·89로 밝기를 고르게 나눴다(간격 49·51·51). 다섯 가지 중 셋
(#f0f0f0·#8c8c8c·#595959)은 이미 쓰이던 값을 그대로 재사용했고, 그림 5·8의
헥스만 이 단계에 맞춰 바뀐다.

적용 범위: 이 팔레트는 "정상 대비 예외"를 나타내는 채움·선에만 쓴다. 실측 대
예측처럼 계열 두 개를 구분하는 회색(그림 3의 "실측" 선, #7a7a7a)이나 지시선·
비강조 글자색(그림 4·6의 회색 글자)은 다른 목적이라 이 팔레트 대상이 아니다 —
b6 의견 필요하면 별도로 다룬다.
"""

INK = "#000000"

BG_WASH = "#f0f0f0"        # 240 — 배경 음영
AUX_GRAY = "#bfbfbf"       # 191 — 보조·부수
EXCEPT_GRAY = "#8c8c8c"    # 140 — 정상 대비 예외
CRITICAL_GRAY = "#595959"  #  89 — 가장 예외적인 것
