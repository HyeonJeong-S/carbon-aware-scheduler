E9 — 서식 일관성 점검 (docx 읽기 전용, 2026-09-21, fe/0c)
CAST.docx를 파싱만 했다. 아무것도 안 고쳤다(도구 규칙: xml.etree, 쓰기 없음).

** 2026-09-21 갱신: 최초 점검은 be의 bac41be/2f1103e 커밋 이전 캐시본
기준이라 일부 낡았었다(문단 인덱스, 5.6절 존재 여부). 라이브(HEAD=2f1103e,
844문단)로 다시 읽어 전부 재확인·수정했다 — 아래 본문이 최신판이다.
앞으로 이 문서 조사는 매번 scratchpad를 지우고 CAST.docx를 새로 unzip
한다(캐시 안 씀). 단, 문단 "인덱스"는 삭제/삽입마다 바뀌므로 내용
스냅샷(문단 텍스트 앞부분)을 같이 적어 나중에 대조 가능하게 한다 —
paraId(w14:paraId) 자체를 뽑아 앵커로 쓰는 건 다음 조사부터 적용한다. **

──────────────────────────────────────────────
우선순위 목록 (급한 순)
──────────────────────────────────────────────

1. ** [해결됨] "문단 1~5" 유령 소제목 — be가 커밋 2f1103e로 통째 삭제 완료 **
   최초 점검(캐시본) 기준 para58·66·77·91·121에서 발견 — 라이브에도 실재를
   확인받았고(be 확인), 문단째 삭제돼 서식 잔류 문제 없이 깔끔하게
   처리됨(849→844문단). 재확인 결과 지금 라이브 문서엔 "문단 N" 패턴이
   0건이다(grep 재확인 완료). 조치 끝 — 더 볼 것 없음.

2. ** 표 캡션과 그림 캡션의 글자 크기가 다르고, 표 캡션은 아예 크기 지정이 없다 **
   그림 캡션(그림1·2·3, para143·429·488): 명시적으로 9pt(sz=18).
   표 캡션(표1~6, 전부): 크기 지정 없음 → 본문 기본값 상속(사실상 10pt로
   보임, docDefaults에 sz 자체가 없어 Word 절대 기본값 10pt).
   즉 지금은 "그림 캡션(9pt) < 표 캡션(10pt 추정)"으로 그림과 표 캡션이
   서로 다른 크기다. C7 규칙 제안("캡션은 표와 같은 크기 또는 본문 크기,
   굵게")과도 안 맞는다 — 지금 캡션은 굵게(bold)도 아니다(확인함, 아래
   3번 참고).
   추천: 캡션 서식을 통일 규칙으로 명시적으로 지정할 것(그림/표 캡션
   전부 같은 크기+볼드).

3. ** C7이 제안한 "표 9pt"가 틀렸다 — 실제 표 데이터는 8pt, 게다가 표마다도 다르다 **
   표1~5 데이터 셀: sz=16 (8pt) — 5개 표가 일치.
   표6 데이터 셀: sz=15 (7.5pt) — 나머지 5개와 다르다.
   지난번 제 C7_표서식_규칙.txt에 "표 9pt(본문 10pt 기준)"라고 제안했는데
   실측해보니 **실제로는 8pt**였다 — 제 제안이 틀렸다. C7 규칙 문서
   정정 필요(이번 보고에서 같이 정정한다 — 아래 "정정" 참고).
   추천: 8pt로 통일 규칙을 다시 쓰고, 표6만 7.5pt인 것도 8pt로 맞출 것.

4. ** 표2(4열, "단계별 총 배출량과 절감률")가 단폭에 거의 딱 맞다 — 여유 없음 **
   2단 조판 계산: 페이지폭 11909twip − 좌우여백 2880twip = 9029twip 본문폭,
   2단 간격 425twip → 단폭 = (9029−425)/2 = **4302 twip**.
   표2의 gridCol 합 = 1700+900+850+850 = **4300 twip**.
   차이 2twip(=0.03pt) — 사실상 여유가 없다. 셀 내부 여백(tcMar)까지 더해
   실제 렌더 폭을 계산 안 했는데, 기본 셀 여백(보통 좌우 108twip씩)까지
   치면 이미 넘칠 가능성이 있다. 다른 표들(표1·3·4·5·6)은 전부 4000~4300
   twip로 표2와 비슷하게 여유가 빠듯하다 — 표2만의 문제가 아니라 **표
   전체가 단폭 한계에 딱 맞춰 만들어진 것으로 보인다.**
   추천: Word에서 실제로 열어 표2·4·5(열이 6개로 제일 빽빽한 것들)가
   실제로 넘치는지 눈으로 확인할 것 — twip 계산만으로는 셀 여백까지
   반영 못 했다.

5. ** [재점검 완료, 라이브 기준] 5.6절/Algorithm 1 — 서식 이상 없음, 깔끔하다 **
   최초 점검 때는 캐시본이라 못 봤던 것 — be 지적대로 실재했다(idx464~479,
   라이브 844문단 기준). 실측 결과:
     - 절 제목 "5.6 용량 인지 온라인 시간 이동"(idx464): Apple SD Gothic
       Neo·22(11pt)·bold·색상 없음 — 기존 소절 제목(예: 옛 "5.6 탄소 회계")과
       **완전히 일치**. 이상 없음.
     - 본문 산문(idx465·466·478 등): 폰트/크기 지정 없음(본문 기본값 상속,
       나눔명조/TNR·10pt) — 다른 본문 문단과 동일. 이상 없음.
     - Algorithm 1 블록(idx477, "Algorithm 1 슬롯 단위 롤링 재평가…"):
       ascii 폰트만 Courier New로 명시(영문·숫자·기호가 고정폭으로 보임),
       eastAsia는 미지정이라 그 안의 한글 단어는 나눔명조로 렌더링됨(의사
       코드+한글 혼용에 흔한 방식). 크기 9pt(sz=18) — 그림 캡션·수식과
       같은 "보조 콘텐츠" 크기 단으로 일관됨. 굵게 아님. w:br(줄바꿈)이
       18개 들어있어 정리.txt가 말한 "19줄 블록"과 정확히 일치 — 한
       문단 안에 텍스트만 이어붙인 게 아니라 실제 줄바꿈으로 의사코드
       형태를 유지하고 있다.
   참고(결함 아님, 스타일 의견): 이 블록에 테두리나 음영이 없어서 본문과
   시각적으로 분리되지 않는다 — Courier New 폰트 전환만으로 "이건 코드"
   라는 게 보인다. 참고 논문들의 알고리즘 박스(보통 테두리 있음)와는
   다르지만, 우리 4편 조사(C1)가 알고리즘 블록 스타일까지는 안 다뤄서
   비교 근거가 약하다 — 굳이 고칠 필요는 없어 보이지만, 박스 테두리를
   추가하면 더 "논문스럽게" 보일 수 있다는 정도로만 남겨둔다. 결정은 c3/be.

──────────────────────────────────────────────
(a) 표 1~7 vs C7 규칙 — 표별 실측
──────────────────────────────────────────────
  (표7=alpha스윕은 아직 docx에 없음 — 새 표라 비교 대상 없음)

  표  캡션                         테두리(가로줄만) 헤더밑줄 데이터폰트 열수/폭(twip)
  1   예측 지평별 NMAE              O(일치)          O        8pt        4열/4100
  2   단계별 총 배출량과 절감률     O(일치)          O        8pt        4열/4300 ⚠단폭한계
  3   지연 등급별 시간이동 기여     O(일치)          O        8pt        5열/4000
  4   사후 용량 강제 리전별 분해    O(일치)          O        8pt        6열/4300 ⚠단폭한계
  5   리전 용량별 시간이동 한계기여 O(일치)          O        8pt        6열/4300 ⚠단폭한계
  6   용량별 리전 배정 작업수       O(일치)          O        **7.5pt**  4열/4300 ⚠단폭한계

  결론: 테두리 스타일(가로줄만, booktabs)은 6개 표 전부 이미 C7 제안과
  일치한다 — 손댈 필요 없음. 캡션 위치도 전부 표 위(이미 "표 제목은 표
  위" 규칙과 일치, 육안 확인함). 실제 문제는 (i) 표6의 폰트 크기 단독
  이탈, (ii) C7 문서 자체가 "9pt"라고 잘못 제안했던 것(정정), (iii)
  단폭 여유 부족.

──────────────────────────────────────────────
(b) 본문 폰트 규칙 위반 — 전수 스캔
──────────────────────────────────────────────
  기본값(docDefaults): ascii=Times New Roman, eastAsia=나눔명조 — be가 말한
  규칙과 일치. 본문 대다수(4717 run)가 이 기본값을 그대로 쓴다(명시적
  재지정 없음) — 위반 없음.

  예외 3종류, 전부 "위반"이 아니라 "의도된 구분"으로 보임:
    - Apple SD Gothic Neo, 9~18pt: 제목(para40)·대소절 제목 32개 — 의도된
      디자인(위 5번 참고). 단, 위 1번의 "문단 1~5" 5개는 예외.
    - sz=16(8pt), 폰트 미지정(기본 상속): 인용 원문 블록("— 원문: ...")과
      표 데이터. 의도된 축소(각주급 크기) — 위반 아님.
    - sz=18(9pt): 그림 캡션 + 수식. 의도된 구분 — 다만 표 캡션과는
      안 맞음(위 2번).

  진짜 폰트 계열 위반(나눔명조/TNR이 아닌 다른 서체)은 못 찾았다 —
  Apple SD Gothic Neo는 "다른 서체를 쓴 위반"이 아니라 "제목용으로
  의도된 두 번째 서체"로 보는 게 맞다(전수 32곳이 전부 실제 절 제목
  자리와 정확히 일치하므로).

──────────────────────────────────────────────
(c) 5.6절·Algorithm 1 — 위 5번 참고(라이브 재점검 완료, 이상 없음)
──────────────────────────────────────────────

──────────────────────────────────────────────
(d) 표2(4열)가 단폭에 들어가는가 — 위 4번 참고(딱 맞음, 위험)
──────────────────────────────────────────────

──────────────────────────────────────────────
[처리 완료] C7_표서식_규칙.txt 정정
──────────────────────────────────────────────
be 확정("캡션 9pt / 표 내부 8pt가 정답")에 맞춰 C7_표서식_규칙.txt를
직접 고쳤다 — "표 9pt" 문구를 "표 내부 8pt·캡션 9pt(그림 캡션과 통일,
표 캡션엔 9pt 명시 필요)"로 교체하고, 테두리 항목도 "CAST.docx 표1~6이
이미 booktabs 방식과 일치함, 손댈 필요 없음"으로 실측 결과 반영해뒀다.
