# -*- coding: utf-8 -*-
"""[7] 3.4절이 비교군임을 못 박는다 — 사용자 지적 중 가장 중요.

사용자: "여기서 왜 리전의 용량을 고려하지 않는거지?? 차라리 그러면 용량인지만
넣는 방식만 넣어도 되는 것 아닌가??"

이 질문이 나왔다는 것 자체가 서술 실패의 증거다. 3.4절은 **비교군**이고 그 결과가
4장의 반사실 상한(65.93%)이 되는데, "3. CAST" 안에 있어 독자가 우리 방법으로 읽는다.
그래서 "왜 우리 방법이 용량을 안 보지?"라는 의문이 생긴다.

세 군데를 고친다.
 1. 절 제목에 "용량 미고려 기준선"을 명시
 2. 첫 문장을 "이 절이 기술하는 것은 비교 대상이다"로 시작 — 읽기 시작하는 순간
    성격을 알리는 것이 요점이다. 끝에 가서 밝히면 이미 오해한 뒤다.
 3. 용량을 안 보는 이유를 "다음 절에서 다룬다"가 아니라 **왜 이 비교군이 필요한가**로
    바꾼다: 이 단계가 만드는 65.93%가 4장의 반사실 상한이고, 그 상한과 실제 값의
    차이가 곧 용량의 대가다.
"""
import shutil
import sys

sys.path.insert(0, "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/tools")
import docx_edit as D

DOC = "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/CAST_압축본.docx"

EDITS = [
    ("3.4 시간 이동 — 마감 내 슬롯 선택",
     "3.4 시간 이동 — 마감 내 슬롯 선택 (용량 미고려 기준선)"),

    ("시간 이동 단계는 작업 j의 실행 가능 윈도우를",
     "이 절이 기술하는 것은 제안 기법이 아니라 그 기준이 되는 비교 대상이다. 각 작업이 "
     "용량을 보지 않고 독립적으로 자기 최적 슬롯을 고르는 경우를 먼저 정의해 두어야, "
     "용량을 강제했을 때 무엇을 잃는지 잴 수 있다. 이 단계가 내는 값이 4장의 반사실 "
     "상한이며, 실제로 달성한 값과의 차이가 곧 용량 제약의 대가다. 용량을 인지하는 "
     "제안 기법은 3.5절에서 기술한다. "
     "시간 이동 단계는 작업 j의 실행 가능 윈도우를 [t_early, t_late] = [s_j, D_j − d_j]로 "
     "둔다. 후보는 이 윈도우 안의 1시간 슬롯이다. 후보를 애초에 마감 이내에서만 탐색하므로 "
     "이 단계에서 마감 위반은 정의상 발생하지 않는다. 윈도우가 비면 즉시 실행한다."),

    ("본 단계는 리전의 용량을 고려하지 않는다.",
     "정리하면 이 기준선은 리전의 용량을 보지 않는다. 후보 슬롯을 고를 때 그 슬롯에 이미 "
     "배정된 작업 수를 참조하지 않고 각 작업이 독립적으로 자신의 최적 슬롯을 고른다. "
     "그래서 저탄소 시간대로 작업이 몰려도 아무것도 막지 않으며, 4.3절에서 보듯 그 결과는 "
     "실행할 수 없는 배치가 된다."),
]


def main():
    shutil.copy2(DOC, DOC.replace("CAST_압축본.docx",
                 "versions/2026-09-24/CAST_압축본_fb7전.docx"))
    z, xml = D.load(DOC)
    n0 = len(D.paragraphs(xml))
    for head, new in EDITS:
        xml, o, n = D.replace_text(xml, head, new)
        print(f"  {o:>4} → {n:>4}자 ({n-o:+5})  {head[:34]}…")
    assert len(D.paragraphs(xml)) == n0
    assert xml.count("<w:drawing>") == 8
    D.save(DOC, z, xml, 0)


if __name__ == "__main__":
    main()
