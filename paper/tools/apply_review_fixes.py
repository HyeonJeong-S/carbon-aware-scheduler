# -*- coding: utf-8 -*-
"""모의 심사 지적 대응 문장 삽입 (2026-09-24).

출처: paper/문서/심사대응_초안.txt (f6 세션). 다만 6번(용량 스윕)은 제안된 문장이
세 수치를 서로 다른 잣대로 센 것이어서 다시 썼다 — cap 별 위반 건수를 각자의 cap
기준으로 세면 무제약은 정의상 0이 되어 "용량을 풀수록 위반이 없어진다"로 읽힌다.
물리적 상한(12건)으로 통일해 다시 재면 무제약이 25,992건이다(직접 재계산 확인).
두 사실을 구분해 쓴다.
"""
import sys
sys.path.insert(0, "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/tools")
import docx_edit as D

P = "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/CAST_압축본.docx"

EDITS = [
    # 1. 미배정 실측치 — §5.4 의 z_j 설명 문단 끝에 붙인다
    ("a_r은 여유율을 적용한 용량에서",
     "a_r은 여유율을 적용한 용량에서 현재 실행 중인 작업 수를 뺀 값이다. 미배정 변수 z_j는 "
     "어떤 리전에도 배정할 수 없을 때 문제가 실행 불가능해지는 것을 막으며, P_slack에 어떤 "
     "배정 비용보다 큰 값을 주어 배정이 가능한 한 선택되지 않도록 한다. 실측에서 미배정은 "
     "0건이다 — 배정 결과 7종 전부에서 146,000건 중 한 건도 발생하지 않았다."),

    # 5. LSTM 제원 — 압축본에는 §5.3 이 없어 LSTM 을 설명하는 자리가 여기뿐이다
    ("본 논문은 탄소 인지 스케줄링 시스템 CAST를 제안한다. CAST는 LSTM",
     "본 논문은 탄소 인지 스케줄링 시스템 CAST를 제안한다. CAST는 LSTM으로 8개 리전의 향후 "
     "24시간 탄소집약도를 예측하고, 정수 선형 계획법 기반 로드밸런서가 실행 리전을 결정한 뒤, "
     "스케줄러가 각 작업의 마감 이내에서 실행 시각을 결정한다. 예측 모델은 리전마다 독립된 "
     "은닉 64·2층 LSTM으로 지난 168시간을 받아 향후 24시간을 낸다. 저장된 체크포인트에는 "
     "가중치만 남아 있어 학습률·에폭·최적화기는 복원하지 못했다. 배치 결정은 예측값으로 하되 "
     "배출량 산정은 실측값으로 하여 예측 오차의 대가가 결과에 반영되도록 했다."),

    # 4. 귀속 명확화
    ("같은 연구는 공간 이득이 낮음에도",
     "Sukprasert 등(EuroSys'24) 6.4절은 공간 이득이 낮음에도 시간 변동이 커 순 절감이 "
     "유의미한 예외로 캘리포니아와 버지니아를 지목하였다. 리전 용량이 유한해 공간 이동이 "
     "조기에 포화되면 부하가 고변동 리전까지 확산되므로, 이 예외가 일반적 상황이 될 수 있다."),

    # 6. 용량 스윕 — 잣대를 명시해 다시 씀
    ("본 연구의 주된 발견은 총계가 아니라 그 분포에 있다.",
     "유효 상한을 바꾸어 보면 이 결과가 상한값에 어떻게 매여 있는지 드러난다. 상한을 6건으로 "
     "조이면 절감률이 58.05%로 내려가고, 마감이 임박해 미룰 수 없는 작업 때문에 그 상한 자체를 "
     "29,962번 넘긴다. 반대로 상한을 풀면 절감률이 65.80%까지 오르지만, 물리적 상한 12건을 "
     "기준으로 다시 세면 위반이 25,992건이다. 본 연구가 쓰는 12건은 그 사이에서 "
     "63.03%·227건에 해당한다. "
     "본 연구의 주된 발견은 총계가 아니라 그 분포에 있다."),
]


def main():
    z, xml = D.load(P)
    n0 = len(D.paragraphs(xml))
    before = sum(len(t) for *_, t in D.paragraphs(xml) if t)
    for head, new in EDITS:
        xml, o, n = D.replace_text(xml, head, new)
        print(f"  {o:>4} → {n:>4}자 ({n-o:+5})  {head[:34]}…")
    after = sum(len(t) for *_, t in D.paragraphs(xml) if t)
    assert len(D.paragraphs(xml)) == n0
    D.save(P, z, xml, 0)
    print(f"\n프로즈 {before:,} → {after:,}자 ({after-before:+,})")


if __name__ == "__main__":
    main()
