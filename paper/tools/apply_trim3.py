# -*- coding: utf-8 -*-
"""10쪽 감축 3차 — §5.1·5.2·5.4·5.6·6.1·6.4 (2026-09-24).

삭제·압축 기준
 1. 캡션이 이미 말한 것을 본문에서 되풀이하는 "그림 N은 ~를 나타낸 것으로" 문단
 2. 절차를 다섯 가지로 늘어놓았으나 바로 아래 수식·알고리즘이 같은 것을 말하는 대목
 3. 방어적 각주성 서술(다른 셈법에서는 성립하지 않는다는 식)
 4. 실행 시간·솔버 등 재현에만 필요한 수치 → 추가자료 A.8 로 이관
정보가 그 문단에만 있는 것은 건드리지 않는다.
"""
import sys
sys.path.insert(0, "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/tools")
import docx_edit as D

P = "/Users/jongha/Desktop/GitHub/carbon-aware-scheduler/paper/CAST_압축본.docx"

EDITS = [
    # ── 캡션 중복 (전량 삭제) ──
    ("그림 3은 α=0(지연 최소)부터 α=1(탄소 최소)까지", None),
    ("그림 6은 이 억제가 하루 중 특정 시간대에", None),

    # (§5.1 개요 문단은 단 구분(sectPr)을 품고 있어 편집 대상에서 제외한다)

    # ── §5.2 표기와 형식화 ──
    ("식 (1)~(5)를 단일 정수 계획 문제로 푸는 것은 두 가지 이유로",
     "식 (1)~(5)를 하나의 정수 계획으로 푸는 것은 규모와 온라인성 때문에 채택하지 않았다. "
     "작업 14만 6천 건에 리전 8개와 24시간 지평을 곱하면 이진 변수가 2,800만 개를 넘고, "
     "작업은 미리 주어지지 않고 시간에 따라 도착하므로 배정 시점에 이후 도착을 알 수 없다."),

    # ── §5.4 공간 이동 ──
    ("공간 이동 단계는 매 슬롯마다 다음 다섯 가지를 수행한다.",
     "공간 이동 단계는 매 슬롯마다 그 슬롯에 제출된 작업을 모으고, 리전별 탄소집약도 예측을 "
     "받아 탄소 비용과 지연 비용을 정규화한 뒤, 정수 계획을 풀어 배정을 확정하고 실행 시간 "
     "동안 해당 리전의 용량에 반영한다."),
    ("탄소 비용만 슬롯별로 정규화하는 것은 슬롯 간 탄소 격차의",
     "탄소 비용만 슬롯별로 정규화하는 것은 슬롯 간 격차의 절대 크기를 가중치 해석에서 "
     "떼어내기 위함이다. 고정된 분모를 쓰면 전 리전이 함께 깨끗한 슬롯에서 탄소 항이 "
     "일률적으로 작아져 같은 가중치가 슬롯마다 다른 뜻을 갖는다."),

    # ── §5.6 용량 인지 온라인 ──
    ("세 가지를 밝혀둔다. 첫째, 우선순위는 탄소 이득이 아니라",
     "세 가지를 밝혀둔다. 우선순위는 탄소 이득이 아니라 남은 여유다 — 탄소 이득으로 "
     "정렬하면 나중에 측정할 지표로 지금 순서를 정하는 순환 논리가 된다. F_r(t)는 P_r(t) "
     "안이 아니라 Q(t) 전체에서 고른다 — 그렇지 않으면 지금이 최선이 아닌 마감 임박 작업이 "
     "누락되어 무한정 밀릴 수 있다. 그리고 |F_r(t)|가 a_r(t)보다 많으면 그 슬롯은 "
     "의도적으로 상한을 넘긴다. **본 설계에서 마감은 하드 제약이고 용량은 소프트 제약이며, "
     "둘이 충돌하면 마감이 우선한다.** 식 (3)은 일반 배정이 지켜야 할 상한이지 예외 없는 "
     "불변식이 아니며, 이 초과는 용량 위반으로 따로 집계한다(§6.4)."),
    ("실측으로는, 공간 이동의 무릎점 탐색(슬롯당 α 후보 11개에 대한 ILP)이", None),

    # ── §6.1 실험 설정 ──
    ("여기서 밝혀둘 점이 있다. 슬롯 길이는 1시간이지만",
     "슬롯 길이는 1시간이지만 온라인 시간 이동은 슬롯 경계가 아니라 제출 시각을 기준으로 "
     "굴러간다. 그래서 지연 예산이 1시간보다 짧은 k=3, 4, 5도 대상에서 빠지지 않는다. "
     "실제로 이 세 등급에서 544건이 지연되었고 마감을 넘긴 작업은 없었다."),

    # ── §6.4 용량 제약과 시간 이동 ──
    ("이 초과가 실제로 얼마나 손해로 이어지는지 가늠하기 위해",
     "이 초과가 얼마나 손해로 이어지는지 가늠하기 위해, 배치 시점에 용량을 전혀 모르는 "
     "대조군을 일부러 만들었다. 배치가 끝난 뒤 상한을 넘긴 작업을 시간 이동 이전으로 "
     "되돌리기를 초과가 사라질 때까지 반복하는 규칙으로, 용량을 아는 스케줄러보다 항상 "
     "같거나 나쁘므로 그 결과는 아무 것도 하지 않았을 때 감수할 손해의 하한이 된다."),
    ("되돌린 작업은 31,250건이다.",
     "되돌린 작업은 31,250건으로 전체의 21.4%다. 시간 이동이 만든 절감은 2,651.6 kg에서 "
     "736.8 kg으로 줄어 총 절감률이 65.93%에서 59.37%가 된다."),
    ("이 하한이 보여주는 손해를 줄이기 위해 용량 인지 온라인 시간 이동을",
     "이 손해를 줄이기 위해 용량 인지 온라인 시간 이동을 구현했다. 슬롯별로 리전 점유량을 "
     "추적해 상한에 도달한 슬롯을 후보에서 미리 제외하는 방식이다. 총 배출량은 10,805.0 kg, "
     "기준 대비 63.03% 감소로 순진한 하한(59.37%)보다 3.66%포인트 더 절감하면서 상한 위반을 "
     "대부분 제거한다 — 최대 동시 실행이 41건에서 17건으로, 캘리포니아의 초과 시간이 "
     "866.9시간에서 7.25시간으로, 프랑스는 622.4시간에서 0.91시간으로 줄어든다(표 1 ④행). "
     "남은 위반 227건은 전량 마감이 임박해 미룰 수 없는 작업이며 알고리즘의 결함이 아니다. "
     "앞서 잰 것이 아무 것도 하지 않았을 때의 하한이고, 이 결과는 그 손해의 대부분을 실제로 "
     "되찾을 수 있음을 보인 것이다."),
]


def main():
    z, xml = D.load(P)
    n0 = len(D.paragraphs(xml))
    before = sum(len(t) for *_, t in D.paragraphs(xml) if t)
    removed = 0
    for head, new in EDITS:
        if new is None:
            xml, o = D.delete(xml, head)
            removed += 1
            print(f"  삭제 {o:>4}자  {head[:32]}…")
        else:
            xml, o, n = D.replace_text(xml, head, new)
            print(f"  수정 {o:>4} → {n:>4}자 ({n-o:+5})  {head[:26]}…")
    after = sum(len(t) for *_, t in D.paragraphs(xml) if t)
    assert len(D.paragraphs(xml)) == n0 - removed
    assert xml.count("<w:drawing>") == 6, "그림 손실"
    D.save(P, z, xml, -removed)
    print(f"\n프로즈 {before:,} → {after:,}자 ({after-before:+,})")


if __name__ == "__main__":
    main()
