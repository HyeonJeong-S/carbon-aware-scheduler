CAST_압축본 그림 처리 체크리스트 (2026-09-25 ~ 09-26)
────────────────────────────────────────
원칙: 그림은 빼지 않고 고친다. 그림 10개 · 번호 1~10 유지.
공통: 10개 모두 이 PC(Windows)에서 다시 생성 → 한글 글꼴 맑은 고딕으로 통일.
      생성 스크립트: paper/diagram/*.py  (Mac 절대경로·Mac 전용 Chrome 경로를 고쳐 두 OS 모두 실행 가능)
      그림 안의 설명 문장(각주)은 모두 캡션으로 옮김 — 그림에는 그림만.

[그림 1] 전체 구조 (gen_figs.py → render_svg.py → fig1_architecture.png)
  [x] 기호를 본문과 맞춤: availᵣ → a_r(t) (식 12)
  [x] 그림 아래 떠 있던 "동시 실행 수 ≤ availᵣ" 삭제 (식 3 과 같은 말)
  [x] 떠 있던 기울어진 리본 "N 리전·시간별" → 탄소 입력 라벨 아래 "리전별 · 1시간"
  [x] "리전 r(j)" 리본이 상자 변을 침범 → 화살표 옆 글자로
  [x] "실행 (리전, 시각)" 리본이 컨테이너 변에 걸침 → 화살표 옆 글자로
  [x] "24 h 곡선" 글자가 상자 변에 겹침 → 아래로
  [x] 선 굵기 낮춤 (컨테이너 1.7→1.1, 상자 1.1→0.8, 화살표 1.3→1.0)
  [=] 유지: 막대 상자(값 하나)·곡선 상자(24h 곡선) 대비, 자리 칸 — 예전 사용자 요청으로 넣은 요소

[그림 2] LSTM 구조 (gen_figs.py → fig2_forecast.png)
  [x] 한때 삭제했다가 사용자 지시로 되살림
  [x] 타일 두 묶음에 "공통 10", "기상 +3" 라벨
  [x] 혼자 떠 있던 "64" → "은닉 64 · 2층"
  [x] "출력 Ĉᵣ" 아래첨자가 Ĉ 와 겹침 → 한 글줄의 아래첨자로
  [x] "탄소집약도 과거 168 h" 가 왼쪽 테두리를 넘음 → 크기·위치 조정
  [x] 선 굵기 1.6→1.0 (그림 1과 통일)
  [x] 본문 3.1절에 구조 문장 추가 (carbon_forecast.py L55-62 근거)
  [x] 캡션 중복 구절 삭제
  [!] 정정: 기상 점선이 출력층으로 들어가는 것은 코드와 맞음(carbon_forecast.py L139, 은닉 상태와
      미래 기상을 이어 붙여 Linear 에 넣음). 처음 검토에서 "틀렸다"고 한 것은 내 오판.

[그림 3] 예측 예시 (gen_figs_data.py → fig3_forecast_curve.png)
  [x] 입력 168h 전체 → 최근 48h 만 (출력 24h 가 가로축 12% → 약 1/3)
  [x] 눈금 -48 … 24 균등화, 캡션 수정

[그림 4] 한 슬롯의 두 축 (gen_figs_data.py → fig3_loadbalancer.png)
  [x] 기타 리전 빈 원 → 작은 회색 점 (범례 "α=1·프랑스" 빈 원과 구별), 범례에 "기타 리전"

[그림 5] 리전 간 흐름 (gen_routing.py → fig4_routing.png)
  [=] 형식 유지 — 예전 사용자 요청(CASPER 재배치 그림 관례)
  [x] 근거 없는 "400건 미만 생략" → "옮긴 물량의 91% 를 차지하는 흐름 10개만" (39개 중)
  [x] 그림 아래 각주 2줄 → 캡션으로 (회색 세로선·아래 상자·검은 상자 기준)

[그림 6] 파레토 곡선 (gen_pareto.py → fig4_pareto.png)
  [x] 곡선은 고정 α 점만 잇고 auto 는 따로 (auto 는 고정 α 곡선 위의 점이 아님)
  [x] 중복 주석·각주 삭제, 캡션 보강

[그림 7] 용량이 막는 방식 (gen_figs_data.py → fig4_scheduler.png)
  [x] 범례를 그림 맨 위 → 아래(동시 실행 수) 패널 안으로, "(그 시각)" 삭제
  [x] 설명 없던 작은 검은 사각형(액자에서 잘린 ▶ 표식) 삭제
  [x] 눈금 0,6,12,18,23 → 0,6,12,18
  [x] "탄소 최저" 글자를 곡선 없는 자리로

[그림 8] 배출량 막대 (gen_comparison.py → fig5_comparison.png)
  [x] 한때 삭제했다가 사용자 지시로 되살림
  [x] 로그 눈금 → 0 에서 시작하는 선형 눈금, 캡션 수정

[그림 9] 동시 실행 추이 (gen_concurrency.py → fig6_concurrency.png)
  [x] 무제약 = 연한 회색 채움, 온라인 = 검은 선 (흑백에서도 구분)
  [x] 숫자 겹침 정리, 각주 → 캡션

[그림 10] 선행 정책 비교 (gen_benchmark.py → fig7_benchmark.png)
  [x] 근거 없는 "위반 300건 이하" 음영 → 본 연구 점을 지나는 점선 2개 + 왼쪽 위 음영
  [x] 눈금 표기 통일(10⁰, 10¹ …), 각주 → 캡션

[그림·표와 본문 사이 간격 (09-26)]
  [x] 빈 줄(빈 문단)로 띄우던 방식 삭제 — 그림 1~4·표 1 만 빈 줄이 있어 들쭉날쭉했음 (9개 삭제)
  [x] 모든 그림·표에 같은 간격 12pt(본문 한 줄 정도): 그림 앞 / 그림 캡션 뒤 / 표 캡션 앞 / 표 뒤 첫 문단 앞
  [x] 표 3 "주:" 는 표에 붙이고(3pt) 그 뒤 12pt
  [x] 캡션 14개 줄 나눔 방지(keepLines), 그림 10개는 캡션과 함께(keepNext)
      — 표 4 캡션이 두 단으로 쪼개져 표와 떨어지던 문제 해결 (¶ 보기 검정 사각형, 인쇄 안 됨)
  [!] 분량 12 → 13쪽 (참고문헌 [6]~[14] 가 13쪽으로 넘어감) → 아래 배치 조정으로 다시 12쪽

[그림 배치 — 단 아래 빈 공간 없애기 (09-26)]
  그림과 캡션은 한 덩어리로 묶어 두었기 때문에, 덩어리가 남은 자리에 안 들어가면 통째로 다음 단으로
  넘어가며 구멍이 생긴다. Word 는 그림을 자동으로 옮겨 주지 않으므로 그림을 본문 몇 문단 뒤로 옮겨
  본문이 먼저 빈자리를 채우게 했다 (캡션은 그림에 붙은 채 함께 이동).
  [x] 그림 2·3 → 3.2절 첫 문단 뒤        (3쪽 오른쪽 단 빈 공간 111pt → 18pt)
  [x] 그림 6 → 3.4절 첫 문단 뒤          (5쪽 202pt·6쪽 324pt 구멍 해소)
  [x] 그림 10 → 표 4 앞                   (11쪽 왼쪽 단 표 4 아래 큰 구멍 해소)
  [=] 9쪽 오른쪽 단 약 3줄 공백은 표 3 덩어리가 넘어가며 생긴 작은 틈 — 유지
  [!] 본문을 고치면 배치가 다시 밀릴 수 있음 — 원고를 확정한 뒤 마지막에 한 번 더 점검할 것

[그림 밖 · 같은 작업 중 처리]
  [x] 표 1~4 가 쪽 경계에서 쪼개지지 않게 묶음 (표 캡션·행에 ¶ 보기 시 검정 사각형 보임, 인쇄 안 됨)
  [x] 식 (3)(7)(11)(13)(14) 9pt, 나머지 10pt. (3)(13)은 가장 길어 번호가 한 줄 아래에 붙음

[아직 안 한 것]
  [ ] 그림 안 글자 영문화 — KTCP 로 간다면 필수(KIISE 투고규정 10항), 학회 확정 후
  [ ] 커밋 (그림 PNG·PDF·SVG, 스크립트 8개, CAST_압축본.docx)
