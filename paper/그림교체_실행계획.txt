그림 교체 실행 계획 — 2026-09-21 (fe/0c 작성, 실제 적용은 be)

전제: docx 조사(캡션.txt 하단 기록)로 확인된 현재 상태.
  - 지금 docx의 그림1~4는 컬러 그림(image1/3/4/5.png)이고, image2.png는 고아(미참조).
  - 새 그림 6장(그림1~6)은 전부 paper/diagram/에 준비 완료 — PNG+원본(SVG 또는 PDF) 둘 다 있음.
  - 이 문서는 계획만 담는다. 실제 word/media 교체·XML 편집은 안 했다(자동작업.md 규칙4:
    편집 전 스냅샷 필수, docx 쓰기는 지금 전면 보류 중).

──────────────────────────────────────────────
0. 공통 — 이미지 크기 규격 (EMU, 1pt = 12700 EMU)
   ** 2026-09-21 3차 갱신 — 배치 규칙 확정(be): "통합(개요) 그림만 전단,
   개별 설명 그림은 단내." 전에 남겨뒀던 "그림2·3 재설계 필요" 숙제를
   이번에 실제로 끝냈다 — 로드밸런서·스케줄러 두 다이어그램을 가로 배치에서
   세로 스택으로 다시 그려 215pt에 9pt 글씨로 넣었다(gen_figs.py). 이제
   전단은 그림1(구조, 세 단계를 한눈에 보여주는 통합 그림) 하나뿐이고
   나머지 5개는 전부 단내로 일관된다 — 아래 표가 최종본이다. **

  그림   파일                      pt 크기      cx(EMU)      cy(EMU)      성격
  그림1  fig1_architecture.png     451×256pt    5,727,700    3,251,200    전단(2단 span, 유일)
  그림2  fig3_loadbalancer.png     215×214pt    2,730,500    2,717,800    단내(1단) — 재설계됨
  그림3  fig4_pareto.png           215×190pt    2,730,500    2,413,000    단내(1단)
  그림4  fig4_scheduler.png        215×240pt    2,730,500    3,048,000    단내(1단) — 재설계됨
  그림5  fig5_concurrency.png      215×184pt    2,730,500    2,336,800    단내(1단)
  그림6  fig6_capacity_sweep.png   215×190pt    2,730,500    2,413,000    단내(1단)

  ※ 표 안 "그림3=fig4_pareto.png"·"그림4=fig4_scheduler.png"는 stage7에서
  번호가 한 번 더 맞교체된 결과다(캡션.txt에 자세히 기록됨) — 파일명 번호와
  논문 번호가 다르니 삽입 때 혼동 주의는 여전히 유효하다.

  해상도: 전부 300dpi 기준. 그림1~4(SVG 다이어그램)는 Chrome headless
  스크린샷 + pymupdf SVG→PDF 변환, 그림5~6(matplotlib)은 savefig dpi=300.
  PNG 픽셀 크기는 각 파일 자체에서 확인 가능(예: fig3_loadbalancer.png =
  897×894px, fig4_scheduler.png = 897×1000px).

──────────────────────────────────────────────
** 4d 대기 작업 (2026-09-21 신규) — 그림2·4 docx 삽입 크기 교체 **
──────────────────────────────────────────────
아래 1번 절의 "그림별 교체표"는 stage4·stage7로 이미 실행 완료된 계획이다
(그림1~6 전부 docx에 삽입·캡션·인용문장까지 들어가 있음, 여러 차례 재확인
완료) — 지금부터는 역사 기록으로만 참고할 것.

새로 생긴 일: 그림2(fig3_loadbalancer.png)·그림4(fig4_scheduler.png) 두
개만 오늘 451pt 전단 → 215pt 단내로 재설계됐다(위 0번 표의 새 pt·EMU 값).
docx 안에는 아직 옛 451pt 크기로, 아마 wp:anchor(2단 span 플로팅)로 들어가
있을 것이다 — 이 둘만 골라서:
  1) word/media 안 그림2·4 이미지를 새 PNG로 교체
  2) <wp:extent cx=... cy=.../> 를 위 표의 새 EMU 값으로 교체
     (그림2: cx=2,730,500 cy=2,717,800 / 그림4: cx=2,730,500 cy=3,048,000)
  3) wp:anchor(전단 플로팅) → wp:inline(단내, 본문 흐름에 낌)으로 전환
     — 2단 조판에서 인라인 이미지는 삽입된 단 안에서만 폭을 차지한다.
  4) 캡션·인용 문장은 이미 있으니 안 건드림 (문장 내용 자체는 안 바뀜,
     그림 생김새만 바뀜 — "가로 배치 박스"에서 "세로 스택 박스"로)
그림1(구조, 전단 유지)·그림3(파레토)·그림5·6은 이번에 안 건드렸으니 그대로.

──────────────────────────────────────────────
1. 그림별 교체표 — 문단 위치 · 지울 구 이미지 · 새 캡션 · 인용 문장
   (역사 기록 — 이미 실행됨, 번호는 stage7 이전 기준이라 지금과 다를 수 있음)

▶ 그림1 (fig1_architecture.png) — 기존 문단 재사용
  옛 자리    : 문단428(그림, rId49→image3.png) / 429(캡션) / 431(인용문장)
  지울 것    : word/media/image3.png (컬러 3단계 박스 그림)
  방법       : rId49의 관계 대상(Target)을 새 파일로 바꾸거나, image3.png 내용
               자체를 fig1_architecture.png로 덮어쓴다(파일명 유지가 더 안전 —
               rId 재연결 없이 그림만 바뀐다). 크기(cx/cy)는 위 표대로 갱신.
  캡션 수정  : 문단429 "그림 2." → "그림 1." (문장 자체는 새로 씀, 캡션.txt 참고)
  인용 문장  : 문단431 "그림 2는 세 단계의 연결과 각 단계가 주고받는 데이터를
               나타낸다." → "그림 1은 …" 번호만 교체(문장 내용은 새 그림에도
               그대로 유효).

▶ 그림2 (fig3_loadbalancer.png) — 신규 삽입
  옛 자리    : 없음. 3.4절(옛 문단 471~492 부근)에 새로 넣는다 — 정확한 삽입
               지점(어느 문단 뒤)은 c3가 3.4절 재작성 시 정할 것.
  필요 작업  : word/media/에 새 파일 추가(예: image6.png로 번호 이어붙임),
               word/_rels/document.xml.rels에 새 Relationship(rId 신규 발급,
               지금 최대가 rId54이므로 rId55부터 쓰면 충돌 없음) 추가,
               새 <w:p><w:drawing>...rId55...</w:drawing></w:p> 삽입.
  캡션       : "그림 2. 공간 이동(로드밸런서) — 슬롯마다 파레토 무릎점으로 α를
               자동 결정해 리전 용량 제약 안에서 작업을 배정하는 슬롯 단위 ILP."
  인용 문장  : 캡션.txt에 초안 있음("그림 2는 슬롯 단위 로드밸런서가…").

▶ 그림3 (fig4_scheduler.png) — 신규 삽입
  옛 자리    : 없음. 3.5절(옛 문단 495~509 부근).
  필요 작업  : 그림2와 동일 패턴, rId56.
  캡션       : "그림 3. 시간 이동(스케줄러) — 마감 인지 탐색 윈도우 안에서
               탄소·지연 가중 점수가 최소인 실행 슬롯을 고른다."
  인용 문장  : 캡션.txt 초안("그림 3은 마감 인지 시간 이동이…").

▶ 그림4 (fig4_pareto.png) — 기존 문단 재사용 + 인용 문장 신규 추가
  옛 자리    : 문단487(그림, rId50→image4.png) / 488(캡션, 인용 문장은 원래
               없었음 — 조사에서 확인).
  지울 것    : word/media/image4.png(구버전 파란 matplotlib 파레토 —
               load_balancer/framework/results/figures/pareto_curve.png와 동일 파일)
  방법       : 그림1과 같은 패턴(rId50 유지, 파일 내용만 교체 추천).
  캡션 수정  : 문단488 "그림 3." → "그림 4."로, 문장도 새로 씀(캡션.txt 참고,
               한계수익 정점 서술 포함).
  인용 문장  : 새로 추가 필요(원래 없었음) — 캡션.txt 초안 사용.

▶ 그림5 (fig5_concurrency.png) — 신규 삽입 (완전히 새 내용)
  옛 자리    : 없음. 4.5절(옛 문단 673~714 부근) — 목차.txt에 이미 위치 명시해둠.
  필요 작업  : rId57 신규.
  캡션 + 인용 문장 : 캡션.txt 초안 사용.

▶ 그림6 (fig6_capacity_sweep.png) — 신규 삽입 (완전히 새 내용)
  옛 자리    : 없음. 4.6절(옛 문단 716~805 부근).
  필요 작업  : rId58 신규.
  캡션 + 인용 문장 : 캡션.txt 초안 사용.

──────────────────────────────────────────────
2. 삭제 대상 구 이미지 정리

  image1.png (구그림1, 8리전 탄소집약도 시계열) — ** 삭제 여부 미결정, 아래 3번 참고 **
  image2.png (고아, LSTM 구조, 참조 0회) — 삭제해도 안전. 아무 문단도 안 건드림.
  image4.png → 그림4로 교체되며 자동 소멸(덮어쓰기 방식이면 별도 삭제 불필요)
  image3.png → 그림1로 교체되며 자동 소멸(동일)
  image5.png (구그림4, 리전별 job수 막대) — ** 삭제 추천, 아래 참고 **
    내용이 표6(문단781, 새 번호 표7 "용량별 리전 배정")과 상당히 겹친다.
    문단575 캡션만 있고 인용 문장이 원래 없어서(=본문이 이 그림을 안 부름)
    지워도 산문 연결이 끊기지 않는다. 다만 최종 확인은 c3/be 몫.

──────────────────────────────────────────────
3. 미결정 — image1(옛 그림1, 탄소집약도 시계열) 처리 방안 (task 3 답변)

** 상황 **: 참고 논문들의 "동기부여 곡선"(CASPER Fig.1, CarbonFlex Fig.1이
서론에서 여는 그림)에 해당하는 자리가 CAST에도 있었다 — 바로 이 옛 그림1이다.
문단145 인용 문장("인도는 석탄 화력 의존도가…")이 서론의 핵심 수치 문장과
같은 자리라, 그림을 완전히 없애도 그 문장 자체는 서론에 남는다(목차.txt
85~86행). 다만 그림 없이 문장만 있으면 "40배 격차"를 독자가 표로도 그림으로도
못 보고 텍스트로만 믿어야 한다.

** 안 A: 흑백 재작성해 그림2로 승격, 기존 그림2(로드밸런서)를 그림3으로 밀기 **
  장점: 참고 논문 관행과 정확히 맞아떨어진다(서론 동기부여 곡선 자리 확보).
        서론이 문장+그림 둘 다 갖춰 설득력이 세진다. 데이터도 이미 있다
        (image1.png가 그리는 원자료 = interface/carbon_2025 또는 동급).
  단점: (a) 그림 번호가 전부 한 칸씩 밀린다 — 목차.txt·캡션.txt·이 문서
        전부 다시 써야 한다(이미 그림4~6까지 다 정해놓은 뒤라 파급이 크다).
        (b) 그림이 7개가 아니라 6개 유지가 목표인데(분량 예산, be가 이미
        "6개로 간다"고 결정), 승격은 그 결정과 충돌한다 — 그림2(로드밸런서)
        를 빼거나 다른 그림을 합쳐야 6개를 유지할 수 있는데, 뺄 만큼
        약한 그림이 없다. (c) 새로 그려야 함(흑백 재작성 작업 추가).

** 안 B: 6개 그대로 유지, image1은 삭제하고 서론은 문장만 (be가 보류시킨 그대로) **
  장점: 이미 내린 결정(6개 유지)과 일치, 추가 작업 없음, 그림4~6 번호도
        안 흔들린다.
  단점: 서론에 참고 논문들이 갖춘 "동기부여 곡선"이 없다 — 심사에서 "왜
        모티베이션 그림이 없냐"는 질문이 나올 수 있다. 다만 문장(40.1배
        격차 실측치)과 5절(관련연구)에서 Sukprasert 인용으로 같은 주장을
        뒷받침하므로 완전히 빈손은 아니다.

** 추천: 안 B (6개 유지) ** — 이유:
  1. be가 그림7(롤링 vs 사전예약)을 분량 예산 때문에 이미 보류했다. 같은
     예산 압박이 여기도 그대로 적용된다 — 7개로 늘리는 안 A는 그 결정과
     바로 충돌한다.
  2. 동기부여 곡선이 "있으면 좋지만 없어도 논증이 무너지지 않는" 종류다
     (문장 인용 + Sukprasert 교차인용으로 이미 뒷받침됨). 반면 그림5·6은
     "그림 없이는 본문 숫자만으로 설득이 약한" 종류다(연속 곡선/체감수익
     패턴은 표보다 그림이 훨씬 더 설득력 있음) — 한정된 그림 예산을 쓸
     우선순위가 다르다.
  3. 안 A를 하려면 지금까지 확정한 그림4~6 번호·캡션.txt·목차.txt·이 문서를
     전부 재작업해야 한다 — 되돌리기 쉬운 결정이 아니다. 반대로 안 B는
     나중에 여유가 생기면 언제든 7번째로 추가할 수 있어 되돌리기 쉽다
     (그림7 보류와 같은 논리를 여기도 적용).

  다만 최종 판단은 be 몫이라 결정하지 않고 추천만 한다.

──────────────────────────────────────────────
4. 적용 순서 제안 (be가 실행할 때)

  1) paper/versions/CAST_YYYYMMDD-HHMM_그림교체.docx 스냅샷(자동작업.md 규칙4)
  2) 그림1·4 먼저(기존 rId 재사용, 파일 덮어쓰기만 — 가장 안전하고 리스크 낮음)
  3) 저장 후 전체 파트 파싱 + mc:Ignorable 확인(규칙4)으로 1차 검증
  4) 그림2·3·5·6 순서로 신규 삽입(각각 문단 위치 확정 후 1개씩, 매번 저장·검증)
  5) 인용 문장 삽입(캡션.txt 초안 기반, c3가 문체 다듬은 뒤)
  6) image1·image2·image5 삭제 여부 최종 확정 후 처리
  7) 표 번호도 같이 밀렸는지(표2 alpha스윕 신규 삽입분) 이 참에 같이 확인
     — 목차.txt 체크리스트에 이미 있는 항목.
