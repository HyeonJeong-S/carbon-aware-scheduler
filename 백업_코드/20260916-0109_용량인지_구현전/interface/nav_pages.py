"""화면 전환 드롭다운에 쓰이는 화면 이름 목록 (app.py, views/main.py가 공유).

app.py는 이 순서대로 PAGES(이름 -> 스크립트 경로)를 만들고,
main.py는 "다른 화면으로 이동" 드롭다운의 선택지로 그대로 쓴다.
"""

NAV_PAGE_NAMES = ("메인 화면", "전체 개요", "로드밸런서", "LSTM", "스케줄러", "최종")
