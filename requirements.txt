# CAST — 저장소 전체 의존성 (대시보드 + 3개 모듈)
#   python -m venv venv && venv\Scriptsctivate && pip install -r requirements.txt

# 공통
numpy>=2.0
pandas>=2.2
plotly>=5.24
dash>=2.18            # 통합 대시보드 (interface/dash_app.py)

# ① carbon-forecast-LSTM (실제 LSTM 예측 — 없으면 더미로 자동 폴백)
torch>=2.0
scikit-learn>=1.3
joblib>=1.3
holidays>=0.40

# ② load_balancer (ILP 라우팅)
pulp>=2.9
matplotlib>=3.8       # results/figures PNG 내보내기

# ③ scheduler (SimPy 이벤트 시뮬레이션)
simpy>=4.0
